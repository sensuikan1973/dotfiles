name: setup machine

on:
  pull_request:
    paths-ignore:
      - '.github/dependabot.yml'
      - '*.md'
  push:
    branches: [main]

jobs:
  setup:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, macos-11]
    timeout-minutes: 20
    steps:
      - name: setup for zsh, Google Cloud SDK
        if: runner.os == 'Linux'
        run: |-
          sudo apt-get update
          sudo apt-get install zsh
          # See: https://docs.brew.sh/Homebrew-on-Linux
          sudo apt-get install build-essential procps curl file git
          # See: https://github.com/sensuikan1973/dotfiles/pull/57#issuecomment-1039400757
          # TODO: ちゃんと update 可能なようにセットアップしてあげる方向で修正したほうがより良い
          sudo apt-get remove google-cloud-sdk
          # ruby
          # TODO: ちゃんとセットアップ
          # See: https://github.com/rbenv/ruby-build/wiki#suggested-build-environment
          sudo apt-get remove ruby
          sudo apt-get install autoconf bison build-essential libssl-dev libyaml-dev libreadline6-dev zlib1g-dev libncurses5-dev libffi-dev libgdbm6 libgdbm-dev libdb-dev
          git clone https://github.com/rbenv/rbenv.git ~/.rbenv
          git clone https://github.com/rbenv/ruby-build.git ~/.rbenv/plugins/ruby-build

      - name: display shell info
        shell: zsh {0} # See: https://github.com/actions/virtual-environments/issues/264#issuecomment-574032011
        run: echo $ZSH_NAME $ZSH_VERSION

      - name: checkout repository to HOME
        shell: zsh {0}
        run: |-
          git clone https://github.com/sensuikan1973/dotfiles.git ~/dotfiles
          cd ~/dotfiles
          # See: https://qiita.com/sonots/items/16df445132e704198e3e
          # See: https://github.com/actions/checkout/issues/23
          git fetch origin '+refs/pull/*:refs/remotes/pr/*' --prune --unshallow
          git checkout $GITHUB_SHA

      # setup twice
      # NOTE: I want to ensure idempotent
      - name: 1st, run setup script
        shell: zsh {0}
        run: cd ~/dotfiles && zsh ./setup.zsh
      - name: 2nd, run setup script
        shell: zsh {0}
        run: cd ~/dotfiles && zsh ./setup.zsh

      - name: rough command test
        shell: zsh {0}
        # See: https://docs.github.com/ja/actions/learn-github-actions/environment-variables
        run: |-
          # set -euxo pipefail # TODO: should run here
          source $HOME/.zshrc
          set -euxo pipefail
          uname
          ls $HOME/dotfiles
          la $HOME/dotfiles
          ll $HOME/dotfiles
          cd $HOME/dotfiles && g status && fp
          upgrade_system_deps $GITHUB_HEAD_REF
          relogin

      # session for debug
      - name: setup tmate session for debug ssh
        if: ${{ failure() }}
        uses: mxschmitt/action-tmate@v3
